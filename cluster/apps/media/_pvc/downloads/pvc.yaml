apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: downloads-volume
  namespace: media
spec:
  storageClassName: longhorn
  volumeMode: Block
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  numberOfReplicas: "1"