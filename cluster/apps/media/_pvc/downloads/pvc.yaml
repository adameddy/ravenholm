apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: downloads-volume
  namespace: media
spec:
  storageClassName: longhorn-rwx
  volumeMode: Block
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi